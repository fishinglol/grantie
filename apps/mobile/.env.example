# Google OAuth client for the phone (Google Cloud console → APIs & Services → Credentials →
# Create credentials → OAuth client ID → type "TVs and Limited Input devices"), in the same
# project as the desktop client. Then restart with: npx expo start -c
# Works in Expo Go; no development build needed.
EXPO_PUBLIC_GOOGLE_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_CLIENT_SECRET=
